// adding and remove element 
let colors: string[] = ['red', 'white', 'blue'];

// adding array with push function
colors.push('yellow');
// adding array with unshift function
colors.unshift('green');

// prop function for removes the last element.
colors.pop();
// shift(): Removes the first element.
colors.shift();

console.log(colors);